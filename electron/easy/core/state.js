// ActiveObject is the object being edited or manipulated
// by the user. This can be an ApplicationSetting, Job,
// BagItProfile, StorageSetting, or null.
var ActiveObject = null;

module.exports.ActiveObject = ActiveObject;
