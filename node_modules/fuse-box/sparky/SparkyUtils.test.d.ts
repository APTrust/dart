export declare class SparkyUtilsTest {
    "Should bump patch"(): void;
    "Should bump minor"(): void;
    "Should bump major"(): void;
    "Should bump next"(): void;
    "Should bump existing next"(): void;
    "Should ignore next when going for patch"(): void;
    "Should ignore next when going for minor"(): void;
    "Should ignore next when going for major"(): void;
}
